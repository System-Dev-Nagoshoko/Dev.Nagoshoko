cat << 'EOF' > src/pages/index.astro
---
import Layout from '../layouts/Layout.astro';

const portalMenus = [
  {
    title: "企画・検索",
    icon: "🔍",
    link: "./search/",
    accent: "#00b4d8", // 鮮やかなブルー
    status: "OPEN",
    wide: false
  },
  {
    title: "熱中症ナビ",
    icon: "☀️", 
    link: "./wbgt/",
    accent: "#ff3366", // 鮮やかなピンク
    status: "LIVE",
    wide: false
  },
  {
    title: "進行予定",
    icon: "🕒",
    link: "#",
    accent: "#74c69d", // 清潔感のあるグリーン
    status: "COMING",
    wide: false
  },
  {
    title: "会場マップ",
    icon: "🗺️",
    link: "#",
    accent: "#7209b7", // ディープパープル
    status: "MAP",
    wide: false
  },
  {
    title: "案内・Q&A",
    icon: "📢",
    link: "./info/",
    accent: "#f77f00", // 温かみのあるオレンジ
    status: "INFO",
    wide: true // 横長フラグ
  }
];
---

<Layout title="【第7回商工祭】案内システム">
  <div id="cyber-splash" class="cyber-splash">
    <div class="loader-content">
      <div class="glitch-title" data-text="SHOKOSAI">SHOKOSAI</div>
      <div class="system-status">
        <span class="scanning">SYSTEM SCANNING...</span>
        <div class="progress-bar"><div class="progress-inner"></div></div>
      </div>
    </div>
  </div>

  <div class="cyber-portal">
    <div class="digital-bg"></div>
    <div class="neon-glow"></div>

    <header class="cyber-header">
      <div class="header-line"></div>
      <h1>第7回商工祭案内システム <span class="ver">v2.0</span></h1>
      <p class="cyber-subtitle">NAGO SHOKO FESTIVAL PORTAL</p>
    </header>

    <main class="portal-container">
      <div class="tile-grid">
        {portalMenus.map((menu) => (
          <a href={menu.link} class={`tile-card ${menu.wide ? 'wide-card' : ''}`} style={`--tile-color: ${menu.accent}`}>
            <div class="tile-edge"></div>
            <div class="tile-icon">{menu.icon}</div>
            <div class="tile-info">
              <span class="tile-status">{menu.status}</span>
              <h2 class="tile-title">{menu.title}</h2>
            </div>
          </a>
        ))}
      </div>
    </main>
  </div>
</Layout>

<script is:inline>
  window.addEventListener('load', () => {
    const splash = document.getElementById('cyber-splash');
    setTimeout(() => {
      splash.style.opacity = '0';
      setTimeout(() => splash.style.display = 'none', 600);
    }, 2200);
  });
</script>

<style is:global>
  /* 🌐 背景をホワイト基調のクリーンなグラデーションに上書き */
  body {
    background-color: #f8fafc !important;
    color: #0f172a !important;
    overflow-x: hidden;
  }
</style>

<style>
  .cyber-portal {
    position: relative;
    min-height: 100vh;
    padding: 20px 16px 100px;
  }

  /* 💎 ライトモード用の方眼グリッド（薄いグレーのグリッド） */
  .digital-bg {
    position: fixed;
    top: 0; left: 0; width: 100%; height: 100%;
    background-image: 
      linear-gradient(rgba(15, 23, 42, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(15, 23, 42, 0.03) 1px, transparent 1px);
    background-size: 30px 30px;
    z-index: -2;
  }

  /* 💡 白背景に映える、爽やかなブルーの淡いオーラ効果 */
  .neon-glow {
    position: fixed;
    top: -50%; left: -50%; width: 200%; height: 200%;
    background: radial-gradient(circle at center, rgba(37, 130, 209, 0.06) 0%, transparent 60%);
    z-index: -1;
  }

  /* 🛰️ スプラッシュは高級感を出すためダークを維持（読み込み後に白へと切り替わります） */
  .cyber-splash {
    position: fixed;
    inset: 0; background: #0a0e17;
    z-index: 10000;
    display: flex; align-items: center; justify-content: center;
    transition: opacity 0.6s ease;
  }
  .loader-content { text-align: center; width: 280px; }
  .glitch-title {
    font-size: 2.5rem; font-weight: 900; letter-spacing: 0.3em;
    color: #00f2ff; position: relative; margin-bottom: 20px;
  }
  .system-status { font-family: monospace; color: #00f2ff; font-size: 0.8rem; }
  .progress-bar { 
    height: 2px; background: rgba(0, 242, 255, 0.2); 
    margin-top: 10px; position: relative; overflow: hidden;
  }
  .progress-inner {
    position: absolute; left: 0; top: 0; height: 100%; width: 100%;
    background: #00f2ff; animation: load 2s infinite ease-in-out;
  }
  @keyframes load { 0% { transform: translateX(-100%); } 100% { transform: translateX(100%); } }

  /* 🏗️ ヘッダー（ダークからシャープな濃紺へ変更） */
  .cyber-header { text-align: center; margin-bottom: 40px; padding-top: 20px; }
  .header-line { width: 40px; height: 4